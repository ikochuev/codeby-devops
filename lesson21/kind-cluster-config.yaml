kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
- role: worker
  extraPortMappings:
  # Порт для доступа к Wordpress извне
  - containerPort: 30080
    hostPort: 30080
    protocol: TCP
- role: worker
